interface:
  display_name: "CopilotKit"
  short_description: "CopilotKit runtime and UI guidance"
  default_prompt: "Use $copilotkit to build or debug a CopilotKit runtime, chat UI, CoAgent flow, or AG-UI integration."
