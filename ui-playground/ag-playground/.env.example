# .NET Business Agent Orchestrator
#
# AG-UI root endpoint exposed by the orchestrator.
# The CopilotKit route uses ORCHESTRATOR_URL on the server.
ORCHESTRATOR_URL=http://localhost:10111

# Browser-visible URL used by the catalog panel on the page.
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:10111
